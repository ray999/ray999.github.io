# ray999.github.io

Personal website built with the Beautiful Jekyll theme.

## Development

Install dependencies and run the site locally:

```bash
bundle install
bundle exec jekyll serve
```

## Updates

- Initialized README with project information.
